# Инструкция по размещению на хостинге

## TunWars: Episode I - Тунисская угроза

### 📋 Быстрый старт

Проект готов к размещению на любом статическом хостинге. Все файлы настроены и не требуют дополнительной конфигурации.

### 📁 Структура проекта

```
Tunis/
├── index.html                                    # Главная страница
├── location.html                                 # Страница деталей локации
├── route.html                                    # Страница маршрута по дням
├── favicon.ico                                   # Иконка сайта
├── photos/                                       # Папка с фотографиями (168 файлов)
├── route/                                        # Данные маршрута
│   ├── hourly_daily_plan.txt
│   └── Revised_Route_Tunisia.md
├── Локации Туниса - Интерактивный справочник_files/
│   ├── locations-data.js                         # Данные всех локаций
│   ├── main.js                                   # Основная логика
│   ├── detailed-descriptions.js                  # Подробные описания
│   ├── leaflet.js                                # Библиотека карт
│   ├── leaflet.css                               # Стили карт
│   └── all.min.css                               # Дополнительные стили
└── Все локации Туниса - Справочник.xlsx          # Исходный Excel файл
```

### 🌐 Варианты размещения

#### 1. GitHub Pages (бесплатно)

1. Создайте репозиторий на GitHub
2. Загрузите все файлы проекта в репозиторий
3. Перейдите в **Settings → Pages**
4. В разделе **Source** выберите:
   - Branch: `main` (или `master`)
   - Folder: `/ (root)`
5. Нажмите **Save**
6. Сайт будет доступен по адресу: `https://ваш-username.github.io/название-репозитория`

**Примечание:** Убедитесь, что все файлы загружены, включая папки `photos/` и `Локации Туниса - Интерактивный справочник_files/`

#### 2. Netlify (бесплатно)

1. Зарегистрируйтесь на [netlify.com](https://netlify.com)
2. Перетащите всю папку проекта в область загрузки на главной странице
3. Сайт автоматически опубликуется и будет доступен по адресу: `https://случайное-имя.netlify.app`
4. Можно настроить кастомный домен в настройках проекта

#### 3. Vercel (бесплатно)

1. Зарегистрируйтесь на [vercel.com](https://vercel.com)
2. Установите Vercel CLI: `npm i -g vercel`
3. В папке проекта выполните: `vercel`
4. Следуйте инструкциям мастера установки
5. Или используйте веб-интерфейс: загрузите папку проекта

#### 4. Обычный хостинг (cPanel, FTP)

1. Подключитесь к серверу по FTP/SFTP (FileZilla, WinSCP и т.д.)
2. Загрузите **все файлы и папки** в директорию:
   - `public_html/` (для основного домена)
   - `public_html/subdomain/` (для поддомена)
   - или указанную администратором
3. **Важно:** Сохраните полную структуру папок!
4. Установите права доступа:
   - Папки: `755`
   - Файлы: `644`
5. Откройте сайт в браузере

### ✅ Проверка после размещения

После загрузки файлов проверьте:

- [ ] Главная страница открывается (`index.html`)
- [ ] Карта загружается и отображает локации
- [ ] Фотографии отображаются на карточках локаций
- [ ] Переход на страницу деталей локации работает (`location.html`)
- [ ] Фотогалерея на странице локации работает
- [ ] Страница маршрута открывается (`route.html`)
- [ ] Интерактивная карта маршрута работает
- [ ] Все ссылки между страницами работают

### 🔧 Технические детали

#### Внешние зависимости (CDN)
- ✅ **Tailwind CSS** - загружается из CDN (автоматически через скрипт)
- ✅ **Font Awesome** - загружается из CDN (cdnjs.cloudflare.com)
- ✅ **Leaflet.js** - для карт (локально + CDN на route.html)
- ✅ **OpenStreetMap** - тайлы карт (загружаются автоматически)

#### Локальные файлы
- ✅ Все фотографии хранятся локально в папке `photos/`
- ✅ JavaScript файлы хранятся локально
- ✅ CSS файлы хранятся локально
- ✅ Данные локаций в формате JSON (locations-data.js)

### 🐛 Решение проблем

#### Карты не загружаются
- **Причина:** Проблема с доступом к OpenStreetMap или Leaflet.js
- **Решение:** 
  - Проверьте консоль браузера (F12 → Console) на наличие ошибок
  - Убедитесь, что `leaflet.js` и `leaflet.css` загружены
  - Проверьте интернет-соединение

#### Фотографии не отображаются
- **Причина:** Файлы не загружены или неправильные права доступа
- **Решение:**
  - Проверьте, что папка `photos/` со всеми файлами загружена
  - Установите права `644` на файлы фотографий
  - Проверьте консоль браузера на ошибки 404

#### Стили не применяются
- **Причина:** Проблема с Tailwind CSS или блокировка CDN
- **Решение:**
  - Проверьте подключение к интернету
  - Проверьте, что скрипт Tailwind загружается (`saved_resource`)
  - Убедитесь, что нет блокировки внешних ресурсов (AdBlock, корпоративный файрвол)

#### Навигация не работает
- **Причина:** JavaScript файлы не загружены
- **Решение:**
  - Проверьте, что файлы `main.js` и `locations-data.js` загружены
  - Проверьте консоль браузера на ошибки JavaScript
  - Убедитесь, что пути к файлам корректны

### 📊 Размер проекта

- **HTML файлы:** ~50 KB
- **JavaScript:** ~200 KB
- **CSS:** ~150 KB
- **Фотографии:** ~50-100 MB (зависит от качества)
- **Общий размер:** ~50-100 MB

### 🔒 Безопасность

- ✅ Все внешние ресурсы используют HTTPS
- ✅ Нет серверных скриптов (статический сайт)
- ✅ Нет обработки пользовательских данных
- ✅ Все пути относительные (работают на любом домене)

### 📝 Дополнительные настройки

#### Для Apache (.htaccess)

Если используете Apache, создайте файл `.htaccess` в корне проекта:

```apache
# Разрешить доступ ко всем файлам
Options -Indexes

# Кэширование статических файлов
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType image/jpeg "access plus 1 year"
    ExpiresByType image/png "access plus 1 year"
    ExpiresByType image/webp "access plus 1 year"
    ExpiresByType text/css "access plus 1 month"
    ExpiresByType application/javascript "access plus 1 month"
</IfModule>

# Сжатие файлов
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/html text/css application/javascript
</IfModule>
```

#### Для Nginx

Добавьте в конфигурацию:

```nginx
location / {
    try_files $uri $uri/ /index.html;
}

# Кэширование
location ~* \.(jpg|jpeg|png|webp|css|js)$ {
    expires 1y;
    add_header Cache-Control "public, immutable";
}
```

### 📞 Поддержка

При возникновении проблем:
1. Откройте консоль браузера (F12 → Console)
2. Проверьте вкладку Network на ошибки загрузки
3. Убедитесь, что структура папок сохранена
4. Проверьте права доступа к файлам

---

**Проект готов к размещению!** 🚀

Все файлы настроены и не требуют дополнительной конфигурации. Просто загрузите их на выбранный хостинг.
