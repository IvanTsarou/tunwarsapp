# 🚀 Размещение проекта на Vercel

## TunWars: Episode I - Тунисская угроза

Vercel — это простой и быстрый способ разместить статический сайт. Есть 3 способа загрузки проекта.

---

## 📦 Способ 1: Загрузка через веб-интерфейс (Самый простой)

### Шаг 1: Регистрация
1. Перейдите на [vercel.com](https://vercel.com)
2. Нажмите **Sign Up**
3. Войдите через GitHub, GitLab, Bitbucket или Email

### Шаг 2: Загрузка проекта
1. После входа нажмите **Add New...** → **Project**
2. Нажмите **Browse** или перетащите всю папку `Tunis` в область загрузки
3. **Важно:** Загружайте всю папку целиком, включая:
   - `index.html`
   - `location.html`
   - `route.html`
   - папку `photos/`
   - папку `Локации Туниса - Интерактивный справочник_files/`
   - папку `route/`
   - все остальные файлы

### Шаг 3: Настройка проекта
1. **Project Name:** Введите имя проекта (например: `tunwars` или `tunisia-locations`)
2. **Framework Preset:** Оставьте **Other** (по умолчанию)
3. **Root Directory:** Оставьте `./` (по умолчанию)
4. **Build Command:** Оставьте пустым (статический сайт)
5. **Output Directory:** Оставьте пустым (корневая директория)

### Шаг 4: Развертывание
1. Нажмите **Deploy**
2. Дождитесь завершения развертывания (обычно 1-2 минуты)
3. Ваш сайт будет доступен по адресу: `https://ваше-имя-проекта.vercel.app`

### Шаг 5: Настройка домена (опционально)
1. В настройках проекта перейдите в **Settings → Domains**
2. Добавьте свой домен (если есть)
3. Следуйте инструкциям по настройке DNS

---

## 💻 Способ 2: Через Vercel CLI (Для разработчиков)

### Шаг 1: Установка Vercel CLI
```bash
npm install -g vercel
```
или через Homebrew (Mac):
```bash
brew install vercel-cli
```

### Шаг 2: Вход в Vercel
```bash
cd /Users/air/Sites/Tunis
vercel login
```
Следуйте инструкциям для авторизации через браузер.

### Шаг 3: Развертывание
```bash
vercel
```

Вам будут заданы вопросы:
- **Set up and deploy?** → `Y` (Да)
- **Which scope?** → Выберите ваш аккаунт
- **Link to existing project?** → `N` (Нет, новый проект)
- **What's your project's name?** → Введите имя (например: `tunwars`)
- **In which directory is your code located?** → `./` (текущая папка)

### Шаг 4: Продакшн развертывание
После первого развертывания, для продакшн версии:
```bash
vercel --prod
```

### Полезные команды:
```bash
vercel ls              # Список всех проектов
vercel inspect         # Детали текущего проекта
vercel remove          # Удалить проект
```

---

## 🔄 Способ 3: Через Git (Рекомендуется для обновлений)

### Шаг 1: Создайте Git репозиторий
```bash
cd /Users/air/Sites/Tunis
git init
git add .
git commit -m "Initial commit: TunWars project"
```

### Шаг 2: Загрузите на GitHub
1. Создайте новый репозиторий на GitHub
2. Выполните:
```bash
git remote add origin https://github.com/ваш-username/название-репозитория.git
git branch -M main
git push -u origin main
```

### Шаг 3: Подключите к Vercel
1. На [vercel.com](https://vercel.com) нажмите **Add New...** → **Project**
2. Нажмите **Import Git Repository**
3. Выберите ваш репозиторий
4. Настройте проект:
   - **Framework Preset:** Other
   - **Root Directory:** `./`
   - **Build Command:** (оставить пустым)
   - **Output Directory:** (оставить пустым)
5. Нажмите **Deploy**

### Преимущества Git способа:
- ✅ Автоматическое развертывание при каждом `git push`
- ✅ История изменений
- ✅ Preview deployments для каждой ветки
- ✅ Откат к предыдущим версиям

---

## ⚙️ Дополнительные настройки

### Файл `vercel.json` (опционально)

Если нужны специальные настройки, создайте файл `vercel.json`:

```json
{
  "version": 2,
  "public": true,
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        },
        {
          "key": "X-Frame-Options",
          "value": "SAMEORIGIN"
        }
      ]
    },
    {
      "source": "/photos/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=31536000, immutable"
        }
      ]
    }
  ],
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
```

**Примечание:** Для нашего проекта `vercel.json` не обязателен, но может улучшить кэширование и безопасность.

---

## 🔍 Проверка после развертывания

После развертывания проверьте:

1. **Главная страница:**
   - Откройте `https://ваш-проект.vercel.app`
   - Проверьте, что карта загружается
   - Убедитесь, что видны локации

2. **Страница локации:**
   - Кликните на любую локацию
   - Проверьте, что фотографии загружаются
   - Проверьте работу фотогалереи

3. **Страница маршрута:**
   - Перейдите на `/route.html`
   - Проверьте интерактивную карту
   - Проверьте раскрытие дней маршрута

4. **Консоль браузера:**
   - Откройте DevTools (F12)
   - Проверьте вкладку Console на ошибки
   - Проверьте вкладку Network на загруженные файлы

---

## 🐛 Решение проблем

### Ошибка: "File too large"
- **Причина:** Некоторые фотографии слишком большие
- **Решение:** Vercel ограничивает размер файлов до 100MB на файл. Уменьшите размер фотографий или используйте Git LFS

### Ошибка: "Build failed"
- **Причина:** Неправильные настройки build команды
- **Решение:** Убедитесь, что **Build Command** пустой, так как это статический сайт

### Фотографии не загружаются
- **Причина:** Путь к файлам неправильный
- **Решение:** Убедитесь, что папка `photos/` загружена полностью

### 404 ошибки
- **Причина:** Файлы не найдены
- **Решение:** Проверьте структуру папок на Vercel в настройках проекта

---

## 📊 Мониторинг и аналитика

Vercel предоставляет:
- **Analytics** — статистика посещений (платная функция)
- **Speed Insights** — анализ производительности
- **Logs** — логи ошибок и запросов

Доступны в панели управления проекта.

---

## 💰 Цены

**Бесплатный план (Hobby):**
- ✅ 100GB пропускной способности
- ✅ Автоматические SSL сертификаты
- ✅ Неограниченное количество проектов
- ✅ Preview deployments
- ✅ Ограничения: 100GB bandwidth/месяц

Для проекта этого размера бесплатного плана достаточно.

---

## 🎉 Готово!

После развертывания ваш сайт будет доступен по адресу:
**https://ваш-проект.vercel.app**

Все изменения будут автоматически развернуты при использовании Git способа.

---

**Нужна помощь?** Обратитесь к [документации Vercel](https://vercel.com/docs)

